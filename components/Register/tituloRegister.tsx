import { StyleSheet } from "react-native";

const tituloRegister = StyleSheet.create({
  tituloRegister: {
    fontSize: 35,
    fontWeight: "700",
    textAlign: "center",
    marginTop: 30,
    marginBottom: 45,
  },
});

export default tituloRegister;
