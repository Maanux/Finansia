import { StyleSheet } from "react-native";

const titleLogin = StyleSheet.create({
  titleLogin: {
    fontSize: 35,
    fontWeight: "700",

    textAlign: "center",
    marginTop: 30,
    marginBottom: 45,
  },
});

export default titleLogin;
