import { StyleSheet } from "react-native";

const singUp = StyleSheet.create({
  singUp: {
    fontSize: 14,
    fontWeight: "500",
    textAlign: "center",
    marginTop: 65,
  },
});

export default singUp;
