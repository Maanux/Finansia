import { StyleSheet } from "react-native";

const inscrever = StyleSheet.create({
  inscrever: {
    fontSize: 14,
    fontWeight: "500",
    textAlign: "center",
    marginTop: 65,
  },
});

export default inscrever;
