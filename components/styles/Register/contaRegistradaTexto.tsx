import { StyleSheet } from "react-native";

const registrada = StyleSheet.create({
  registrada: {
    fontSize: 14,
    fontWeight: "500",
    textAlign: "center",
    marginTop: 65,
  },
});

export default registrada;
